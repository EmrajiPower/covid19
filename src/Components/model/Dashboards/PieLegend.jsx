import React from "react";

import AvatarIconModel from "../Buttons/AvatarIconModel";

function PieLegend(props) {
  return (
    <div>
      <AvatarIconModel {...props} />
    </div>
  );
}

export default PieLegend;
